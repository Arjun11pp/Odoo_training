<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="view_account_payment_form_inherit_combined_payment" model="ir.ui.view">
        <field name="name">view.account.payment.form.inherit.payment</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='memo']" position="after">
                <field name="invoice_ids" domain="[('move_type', 'in', ['out_invoice', 'out_refund', 'out_receipt']),('partner_id','=',partner_id)]">
                    <list editable="bottom">
                        <field name="name"/>
                        <field name="partner_id"/>
                        <field name="amount_total_in_currency_signed"/>
                        <field name="final_invoice_amount"/>
                    </list>
                </field>
            </xpath>
        </field>
    </record>

</odoo>